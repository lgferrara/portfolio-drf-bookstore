DJANGO_SETTINGS_MODULE=config.settings.demo

# Optional for demo (base already provides a dummy):
# DJANGO_SECRET_KEY=django-insecure-demo-key-not-for-production

# For prod-like:
# DJANGO_SECRET_KEY=<generate with the command below>
# python -c "from django.core.management.utils import get_random_secret_key as g; print(g())"

# The ones below are only used with prod.py
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_CSRF_TRUSTED=http://localhost:8000
# DATABASE_URL=postgres://user:pass@host:5432/dbname
# DJANGO_EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# DJANGO_DEFAULT_FROM_EMAIL=portfolio-demo@example.com
